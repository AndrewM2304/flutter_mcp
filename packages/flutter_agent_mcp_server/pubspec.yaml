name: flutter_agent_mcp_server
description: In-house MCP stdio server for Flutter agent runtime inspection.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.3.0 <4.0.0"

dev_dependencies:
  lints: ^4.0.0
  test: ^1.25.0
